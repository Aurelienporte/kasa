function Slideshow ({pictures, title}){
    return(
        <div className="carrousel">
            ici un carrousel
            {/* {pictures.map(picture => <img src={picture} alt={title}/>)} */}
        </div>
    )
}
export default Slideshow